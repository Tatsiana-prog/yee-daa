import { ArrowBackIcon, ArrowForwardIcon } from '@chakra-ui/icons';
import { Icon } from '@chakra-ui/react';

export const ArrowLeftIcon = () => <Icon as={ArrowBackIcon} />;

export const ArrowRightIcon = () => <Icon as={ArrowForwardIcon} />;
